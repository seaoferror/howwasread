name: offlineconversation
on:
  push:
    branches:
      - main
    paths:
      - 'backend/offlineconversation/**'
      - '.github/workflows/offlineconversation.yml'

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: checkout github
        uses: actions/checkout@v6

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: build and push offlineconversation
        env:
          REGISTRY: ghcr.io
          REPOSITORY: ${{ github.repository_owner }}/backend/offlineconversation
          IMAGE_TAG: latest
        run: |
          IMAGE_NAME=$(echo "$REGISTRY/$REPOSITORY:$IMAGE_TAG" | tr '[:upper:]' '[:lower:]')
          
          docker buildx build --platform linux/amd64 \
          -t $IMAGE_NAME \
          -f ./backend/offlineconversation/Dockerfile ./backend/offlineconversation --push
