apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: namespace-waypoint
  namespace: backend
  annotations:
    istio.io/waypoint-for: name
spec:
  gatewayClassName: istio-waypoint
  listeners:
    - name: mesh
      port: 15008
      protocol: HBONE