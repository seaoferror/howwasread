apiVersion: v1
kind: ConfigMap
metadata:
  name: gpu-slicing-config
  namespace: kube-system
data:
  config.yaml: |-
    version: v1
    sharing:
      timeSlicing:
        resources:
          - name: nvidia.com/gpu
            replicas: 10