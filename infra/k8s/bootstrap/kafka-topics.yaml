apiVersion: kafka.strimzi.io/v1
kind: KafkaTopic
metadata:
  name: chat-message
  namespace: kafka-system
  labels:
    strimzi.io/cluster: kafka-cluster
spec:
  partitions: 3
  replicas: 1

---

apiVersion: kafka.strimzi.io/v1
kind: KafkaTopic
metadata:
  name: search
  namespace: kafka-system
  labels:
    strimzi.io/cluster: kafka-cluster
spec:
  partitions: 3
  replicas: 1