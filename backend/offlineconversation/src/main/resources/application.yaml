server:
  port: 8080
spring:
  threads:
    virtual:
      enabled: true
  application:
    name: offlineconversation
  config:
    import: optional:file:env.properties
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://gateway01.ap-northeast-1.prod.aws.tidbcloud.com:4000/development?sslMode=VERIFY_IDENTITY
    username: 3aeV1qGntiqNDfJ.root
    password: ${DB_PASSWORD}
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
    generate-ddl: true
    hibernate:
      ddl-auto: update
    show_sql: true
    properties:
      hibernate:
        default_batch_fetch_size: 1000
logging:
  level:
    org.springframework.web.socket: INFO
    org.springframework.messaging: INFO